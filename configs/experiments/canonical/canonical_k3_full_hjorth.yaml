experiment:
  name: "canonical_k3_full_hjorth"
  seed: 42

windowing:
  window_ms: 300
  step_ms: 100
  edge_trim: 1

paths:
  features_dir: "data/interim/features/task_hjorthonly/"
  output_dir: "outputs/experiments/canonical/"

features:
  use_theta: false
  use_alpha: false
  use_beta: false
  use_hjorth: true
  use_entropy: false

pipeline:
  pca:
    variance_retained: 0.90

  hmm:
    k_states: 3
    covariance_type: "full"
    n_iter: 500
    tol: 0.001
